export interface cliente {		

    createdDate: Date;
    dataBase: string;
    id: string;
    name: string;
    host: string;
    updatedDatabase: boolean;
    updatedDate: Date;
    active: boolean;

}